{% macro content(spec) %}

{% set spec = spec or {} %}

<div class="bandera">
  <h2>{{ spec.nombre}}</h2>
  <p>Población: {{ spec.poblacion }}</p>
  <img src="images/{{ spec.img }}" alt="">
</div>
{% endmacro %}
